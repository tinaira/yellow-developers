---
Title: Yellow themes
Sidebar: sidebar
Language: en
---
You can customise the visual design of your website.

## Themes

Themes define the visual design of your website. Start with a [theme](/themes/) that you like. There's a default theme defined in file `system/config/config.ini`. A different theme can be defined in the [settings](markdown-cheat-sheet#settings) at the top of each page, for example `Theme: default`.

## Custom theme

To use a custom theme set the `Theme` setting at the top of a page. You can also create a theme with the name of a `content` folder and it will be automatically used. A theme consists of a [style sheet](https://en.wikipedia.org/wiki/Cascading_Style_Sheets) and additional files.
 
Here's an example theme file `system/themes/home.css`:

```
/* Theme: Home theme */
/* Version: 0.1.1 */
/* Designer: Mark Mayberg */

html, body, div, form, pre, span, tr, th, td { margin:0; padding:0; border:0; vertical-align:baseline; }
body { background-color:#fff; color:#717171; margin:1em; }
h1, h2, h3, h4, h5, h6 { color:#07d; font-weight:normal; }
a { color:#07d; text-decoration:none; }
a:hover { color:#07d; text-decoration:underline; }
```

## JavaScript

To extend a theme you have the possibility to use [JavaScript](http://en.wikipedia.org/wiki/JavaScript). You can create a script with the name of a theme and it will be included together with the theme. This allows you to create any dynamic feature with Yellow.

Here's an example JavaScript file `system/themes/assets/home.js`:

```
var ready = function() 
{
	console.log("Hello world");
	// Add more JavaScript code here
}
if(window.addEventListener) {
	window.addEventListener("load", ready, false);
} else {
	window.attachEvent("onload", ready);
}
```

## Site icon

The site icon, also called favicon, is a small image that is associated with a website. There's a default icon defined in file `system/config/config.ini`. A different icon can be defined in the [settings](markdown-cheat-sheet#settings) at the top of each page, for example `Siteicon: icon`.

To change the site icon overwrite file `system/themes/assets/icon.png`.

[Next: Yellow templates →](yellow-templates)
